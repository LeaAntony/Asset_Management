@using System.Globalization;
@using Microsoft.AspNetCore.Http;
@using Asset_Management.Controllers;
@using Asset_Management.Function;
@model List<GPOpenModel>
@{
    string department = ViewBag.department;
}
<style>
    .dataTables_info, .dataTables_paginate, .dataTables_filter {
        font-size: 12px;
        color: var(--text-light);
    }

    @@media screen and (max-width: 768px) {
        .modal-body {
            padding: 16px;
        }

        .professional-table {
            font-size: 11px;
        }

            .professional-table thead th {
                padding: 12px 8px;
                font-size: 10px;
            }

            .professional-table tbody td {
                padding: 8px;
            }
    }

    @@media screen and (max-width: 576px) {
        .modal-header {
            padding: 16px 20px 12px;
        }

            .modal-header .modal-title {
                font-size: 16px;
            }

        .modal-body {
            padding: 12px;
        }

        .professional-table {
            font-size: 10px;
        }

            .professional-table thead th {
                padding: 8px 6px;
                font-size: 9px;
            }

            .professional-table tbody td {
                padding: 6px;
            }
    }
</style>
<div class="modal-header">
    <h5 class="modal-title"><i class="fas fa-list"></i> Gate Pass OPEN List - @department</h5>
    <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>
<div class="modal-body">
    <div class="row">
        <div class="col-12">
            <table class="table table-sm professional-table align-center nowrap" id="table_gp_open">
                <thead>
                    <tr>
                        <th hidden>ID Gatepass</th>
                        <th>Request ID</th>
                        <th>Category</th>
                        <th>Gate Pass Date</th>
                        <th>Requestor Name</th>
                        <th>Return Date</th>
                        <th>Destination</th>
                        <th>New Pic Name</th>
                        <th>Status</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>                    
                    @foreach (var row in Model)
                    {
                        <tr>
                            <td hidden>@row.id_gatepass</td>
                            <td>@row.gatepass_no</td>
                            <td>@row.category</td>
                            <td>@row.gatepass_date</td>
                            <td>@row.requestor_name</td>
                            <td class="@(row.return_date == "-" ? "text-center" : "")">@row.return_date</td>
                            <td class="@(row.destination == "-" ? "text-center" : "")">@row.destination</td>
                            <td class="@(row.new_pic_name == "-" ? "text-center" : "")">@row.new_pic_name</td>
                            <td>@row.status_gatepass</td>
                            <td>
                                <button class="btn btn-info btn-sm" title="Open Detail" onclick="open_detail('@row.id_gatepass')">
                                    <i class="far fa-list-alt"></i>
                                </button>
                                @if (row.category == "FA SALES - WITHOUT DISMANTLING")
                                {
                                    <button class="btn btn-secondary btn-sm" title="View Image" onclick="gatepass_img('@row.id_gatepass')">
                                        <i class="fa fa-image"></i>
                                    </button>
                                }
                                else if (row.category == "REPAIR TO VENDOR")
                                {
                                    <button class="btn btn-secondary btn-sm" title="View Image" onclick="repair_img('@row.id_gatepass')">
                                        <i class="fa fa-image"></i>
                                    </button>
                                }
                                else
                                {
                                    <button class="btn btn-secondary btn-sm" title="View Image" onclick="show_img('@row.id_gatepass')">
                                        <i class="fa fa-image"></i>
                                    </button>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
<script>
    $(function () {
        const $tbl = $("#table_gp_open");

        const dt = $tbl.DataTable({
            scrollX: true,
            scrollCollapse: true,
            autoWidth: false,
            deferRender: true
        });

        const $modal = $tbl.closest(".modal");
        $modal.on("shown.bs.modal", function () {
            dt.columns.adjust();
        });

        dt.on("init.dt", function () {
            dt.columns.adjust();
        });
    });
</script>